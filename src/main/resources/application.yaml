spring:
  jackson:
    # snake case style for response json
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: NON_NULL

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/l23o6
    username: root
    password: root

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: create-drop


# sa-token:
#   token-name: token
#   # 30 days
#   timeout: 2592000
#   # no expiration
#   activity-timeout: -1
#   # multi login
#   is-concurrent: true
#   is-share: true
#   token-style: uuid
#   is-log: true
#   is-read-cookie: false